export type UserPayload = {
    username: string
    password: string
    fullname: string
}

export type User = {
    id: string
    username: string
    password: string
    fullname: string
}
